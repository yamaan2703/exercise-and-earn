export enum StatusUser {
  ACTIVE = "Active",
  INACTIVE = "Inactive",
}

export enum StatusProduct {
  ACTIVE = "Active",
  INACTIVE = "Inactive",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum UserDetailTab {
  PROFILE = "profile",
  CALORIES = "calories",
  PRODUCTS = "products",
}
