export enum Status {
  ACTIVE = "Active",
  INACTIVE = "Inactive",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum UserDetailTab {
  PROFILE = "profile",
  CALORIES = "calories",
  PRODUCTS = "products",
}
